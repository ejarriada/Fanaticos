import React from 'react';

const ProductReservationList = () => {
    return (
        <div>
            <h2>Lista de Reservas de Productos</h2>
        </div>
    );
};

export default ProductReservationList;