import React from 'react';
import ProductReservationList from './ProductReservationList';

const ProductReservationManagement = () => {
    return (
        <div>
            <h1>Gestión de Reservas de Productos</h1>
            <ProductReservationList />
        </div>
    );
};

export default ProductReservationManagement;